# extreme_svd_project
# benchmark using pyparsvd

# In the repository, it's equipped with several different options to generate datasets

- data_download : Using cdsapi to fetch atmosphere dataset
- data_generate : Using numpy random to generate matrix in format HDF5
- data_mini : Using the method provided in library (solutions to an equation) to generate in format HDF5
  

